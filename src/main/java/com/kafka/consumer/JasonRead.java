package com.kafka.consumer;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

public class JasonRead {
	private static final Logger log = LoggerFactory.getLogger(JasonRead.class);

	public static void readJson(String jsonStr) {
		log.info("In Json Read Method Start");
		
 
		try {
			
		//	System.out.println("Emp Name "+name); 
		//	 JsonNode idNode = node.get("id"); 
			// String id =
			/* * idNode.asText(); System.out.println(id); JsonNode salNode = node.get("sal");
			 * Double sal = salNode.asDouble(); System.out.println(sal); JsonNode array1 =
			 * node.get("message"); JsonNode jsonNode1 = array.get(1); String techStr =
			 * jsonNode.asText(); System.out.println(techStr);
			 */

		} catch (Exception e) {
			e.printStackTrace();
		}
	}

}
